enum VehicleCategory { compact, sports, muscle, hypercar, suv, truck }

enum VehicleRarity { common, rare, epic, legendary, mythic }

enum CurrencyType { coins, gems, fuel, eventTokens }

enum GameMode {
  endlessHighway,
  timeTrial,
  policeEscape,
  survival,
  deliveryRush,
  driftChallenge,
  ghostMode,
}

enum MapTheme {
  cityHighway,
  desert,
  snowMountain,
  neonCyberCity,
  forestHighway,
  coastalRoad,
}

enum WeatherType { clear, rain, fog, snow, sandstorm, night }

enum ControlScheme { tilt, touchSteering, buttons, swipeLane }

enum MissionType { daily, weekly, seasonal, achievement }

enum MissionStatus { active, completed, claimed, expired }

enum AdType { rewarded, interstitial, appOpen, banner }

enum AuthProvider { guest, google }
